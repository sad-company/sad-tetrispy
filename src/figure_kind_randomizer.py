from figure_kind import FigureKind


class FigureKindRandomizer:
    # TODO: FigureKindRandomizer должен знать из каких типов
    #      фигур он будет генерировать.
    #      Подумай, сам рандомайзер сразу не знает,
    #      из каких типов фигур он будет генерировать,
    #      то откуда он может узнать их список?
    # TODO: Подумай, нужно ли хранить прошлое/будущее сгенерированное значение
    #      (подсказка - подумай про то, что тетрис должен подсказывать след фигуру)

    def get_random(self) -> FigureKind:
        # TODO(AB): Task for Serg
        pass
